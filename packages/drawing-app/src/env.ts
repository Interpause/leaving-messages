import { createShapeId } from 'tldraw'

export const DARK_MODE = false
export const QUERY_PARAM_DOC = 'doc'
export const BACKEND_URL = window.location.origin
export const CANVAS_PROPS = { w: 512, h: 512, name: 'Draw Here!' }
export const FRAME_ID = createShapeId('frame')
